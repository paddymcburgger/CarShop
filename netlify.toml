[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA-style fallback for dynamic routes
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/ssr"
  status = 200
